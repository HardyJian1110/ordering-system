<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.MenuMapper">


    <select id="selectByRole" resultType="com.sky.entity.Menu">
        select * from employee_menu left join menu on menu.id = employee_menu.menu_id
        where employee_menu.role = #{role}
    </select>

</mapper>
